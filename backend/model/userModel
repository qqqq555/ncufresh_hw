const db=require('../ncufreshHW.db');
const userModel={
    add:(user, cb)=>{
        db.query(
            'INSERT INTO accounts(account, password) VALUES(?, ?)',
            [account, password],
            (err, results)=>{
                if(err) return cb(err);
                cb(null);
            }
        );
    },

}